- Install a python interpreter
- Install pip and add the python path in environmental variables
- Install the below packages
   - pip install opencv-python
   - pip insatll numpy
   - pip install scipy
- Then run carttonify.py for cartoonify the image. You can change the input image in the last second line
- You can run video_cartoonize.py for live cartoonify the video.
